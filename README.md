# HTML5 Boilerplate remix

`/boilerplate` contains the "Boilerplate" promoted by the project.

contents one level above boilerplate is built using the boilerplate but customized for the demonstration

